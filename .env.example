# ==================== Supabase Configuration ====================
#
# How to get these values:
# 1. Go to https://supabase.com/dashboard
# 2. Select your project
# 3. Settings > API
#

# Project URL (e.g., https://abcdefghijk.supabase.co)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Anon Key (Public - safe to use in frontend)
# Settings > API > Project API keys > anon public
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...your-anon-key

# Service Role Key (Secret - for Edge Functions deployment)
# Settings > API > Project API keys > service_role secret
VITE_SUPABASE_ACCESS_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...your-service-role-key

# Project Reference ID (e.g., oapwrpmohheorgbweeon)
# Settings > General > Reference ID
SUPABASE_PROJECT_REF=your-project-ref

# ==================== App Configuration ====================

# App name displayed in UI
VITE_APP_NAME=Character Battle

# ==================== Google OAuth Configuration ====================
#
# Google OAuth is configured in Supabase Dashboard:
# 1. Authentication > Providers > Google > Enable
# 2. Get Client ID and Secret from Google Cloud Console
# 3. Add Authorized redirect URIs:
#    - https://your-project-ref.supabase.co/auth/v1/callback
#
# No additional env vars needed for frontend (handled by Supabase)
#

# ==================== Optional: AI API Keys (for Edge Functions) ====================
#
# If using AI services in Edge Functions, set these secrets:
# supabase secrets set OPENAI_API_KEY=your-key
# supabase secrets set ANTHROPIC_API_KEY=your-key
#
# Note: These are NOT set in .env, but via Supabase Secrets
#
